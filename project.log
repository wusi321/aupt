[2026-04-27 00:00:00] 初始化 `aupt/` 工程结构，创建核心目录、CLI、dispatcher、distro detector、package resolver、mirror manager、基础 backend、数据库样例、测试与项目入口，目标为实现统一 Linux 包管理基础能力。
[2026-04-27 00:20:00] 修复 `aupt/cli/commands.py`、`aupt/core/dispatcher.py`、`aupt/core/mirror_manager.py` 中批量生成导致的换行字符串断裂问题，补充 `Path` 导入，并将镜像测速超时配置贯通到自动选源流程。
[2026-04-27 00:28:00] 修复 `aupt/core/config_manager.py` 中配置文件写入换行字符串断裂问题，避免 `doctor` 与 `config` 命令在导入阶段触发语法错误。
[2026-04-27 00:42:00] 补充 `scripts/install.sh`、`scripts/uninstall.sh`、`README.md` 与 `pyproject.toml` 打包配置，支持基于系统 `python3` 的用户级/系统级安装，并通过 `console_script` 生成全局命令 `aupt`。
[2026-04-27 00:55:00] 修复安装后 `aupt` 无法读取数据库的问题：将 `build_dispatcher()` 的数据库定位改为优先使用已安装包目录，新增 `aupt/database/__init__.py`，并在 `pyproject.toml` 中声明打包 `database/*.json` 资源文件。
[2026-04-27 01:02:00] 修复 `aupt/utils/logger.py` 在只读文件系统上初始化日志文件失败的问题，改为文件日志不可写时自动回退为仅控制台输出，保障全局命令可执行。
[2026-04-27 01:10:00] 优化 `aupt/backends/apt_backend.py`：将脚本型操作切换为 `apt-get`，查询类命令切换为 `apt-cache`，减少 `apt` CLI 稳定性警告；同步更新 `README.md` 中的 APT 后端说明。
[2026-04-27 01:20:00] 完善 `git.sh` 一键提交脚本：支持自动定位项目根目录、自动初始化 Git、自动创建或更新 `origin`、无变更跳过提交、自定义提交信息，以及可选 `--force` 强制推送。
[2026-04-27 01:35:00] 修复 `aupt mirror auto` 在非 root 环境下写 `/etc/apt/` 时抛出 `PermissionError` traceback 的问题：在 `mirror_manager.py` 新增 `_write_mirror_config()` 统一处理权限并给出友好提示，在 `dispatcher.py` 的 `_handle_mirror()` 加 `try-except` 兜底。
[2026-04-27 02:00:00] 新增交互式镜像选择功能：创建 `aupt/utils/mirror_selector.py` 实现带超时的镜像菜单，集成到 `dispatcher.py` 每次 `install` 命令前自动触发，集成到 `scripts/install.sh` 每次安装 AUPT 前自动触发 pip 镜像选择，默认超时 15s 自动选最佳镜像。
[2026-04-27 02:30:00] 修复 `scripts/install.sh` 交互菜单中 stdout/stderr 混用导致 `$()` 捕获到提示文本产生语法错误的问题：将 `read_pip_choice`、`select_pip_mirror`、`pip_mirror_auto` 的提示信息全部重定向到 stderr，stdout 仅保留纯返回值。
[2026-04-27 02:45:00] 修复 `pip_mirror_auto()` 对部分镜像源（aliyun/ustc）测速卡住的问题：curl 增加 `--max-time 5` 限制整个请求超时，awk 比较改用 `LC_ALL=C -v` 传参避免 locale 解析异常。
[2026-04-27 03:00:00] 同步 `install_compat.sh` 低版本安装脚本：新增 `MIRROR_TIMEOUT`/`PIP_MIRRORS` 全局变量、`select_pip_mirror`/`pip_mirror_auto`/`read_pip_choice` 三个交互函数，`install_aupt()` 中注入 pip 镜像选择，stdout/stderr 分离与 `install.sh` 保持一致。
[2026-04-27 03:15:00] 修复三个 CLI 问题：(1) `--version`/`-V`/`--help`/`-h` 不再报"不支持的 action"；(2) `Ctrl+C` 打断 `update`/`upgrade` 等长命令时不再泄露 Python traceback，在 `subprocess_wrapper.run_command()` 和 `commands.main()` 两层捕获 `KeyboardInterrupt`。
[2026-04-27 03:30:00] 修复 `update`/`upgrade`/`install`/`remove`/`clean` 命令运行时看不到任何进度输出的问题：`subprocess_wrapper.run_command()` 新增 `stream` 参数，`capture_output=False` 时输出直接流向终端；`base_backend.execute()` 透传 `stream`；五个长时方法设置 `stream=not dry_run`。
